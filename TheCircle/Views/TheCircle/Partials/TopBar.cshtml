<nav class="navbar navbar-fixed-top navbar-top" style="background-color:#901F61">
    <style>
        #logout {
            color: #901F61;
            height: 50px;
            width: 50px;
            color: #fff;
        }
        #logout button {
            background-color: inherit;
            border: none;
            position: relative;
            height: 50px;
            width: 50px;
        }

        #contenedor {
            color: #901F61;
        }

    </style>
    <div id="contenedor" class="container-fluid">

        <div class="navbar-header" >
            <a onClick="document.location.reload();" class="navbar-brand">
                <span style="color:white">TheCircle</span>
            </a>
            <button type="button" class="sidebar-toggle" style="color:white" onclick="change()">
                <span id="spanid" class="glyphicon glyphicon-chevron-left"></span>
            </button>
        </div>

        <ul class="nav navbar-nav navbar-right">
            <li class="dropdown">
                <div id="logout" class="dropdown-toggle" data-toggle="dropdown">
                    <button data-ripple><span class="glyphicon glyphicon-user"></span></button>
                </div>

                <ul class="dropdown-menu dropdown-menu-profile">
                    <li class="dropdown-header">Detalles de la cuenta</li>
                    <li>
                        <div class="media">
                            <div class="media-left">
                                <img ng-src="{{session_foto}}" alt="avatar" class="media-object img-circle">
                            </div>
                            <div class="media-body">
                                <h5 class="media-heading">Edgar Fernando C U{{session_name}}</h5>
                                <p>blabla@childre.org{{session_email}}</p>
                            </div>
                        </div>
                    </li>
                    <li class="divider"></li>
                    <li>
                        <a data-ripple onClick="document.location.replace('logout');" style="cursor: pointer;">
                            Log out  <span class="glyphicon glyphicon-log-out"></span>
                        </a>
                    </li>
                </ul>
            </li>
        </ul>

    </div>
    <script type="text/javascript">
        function change() {
            if (document.getElementById("spanid").className == "glyphicon glyphicon-chevron-right") {
                document.getElementById("spanid").className = "glyphicon glyphicon-chevron-left";
            } else {
                document.getElementById("spanid").className = "glyphicon glyphicon-chevron-right";
            }
        }
    </script>
</nav>